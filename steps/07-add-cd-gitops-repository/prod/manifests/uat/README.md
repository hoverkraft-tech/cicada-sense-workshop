# UAT raw manifests

This folder contains raw Kubernetes manifests for the user acceptance testing environment.

Store UAT-specific resources here when they are managed directly as manifests instead of being fully expressed through Helm values or higher-level ArgoCD application configuration.

Use this folder for release validation resources before promotion to production.
