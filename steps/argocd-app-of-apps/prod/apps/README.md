# ArgoCD Apps for prod env

Here you can put ArgoCD applications manifests to be deployed to prod cluster.
Note that the name must be unique through all clusters since it will be deployed to the cluster where ArgoCD lives in the namespace `argocd`.
