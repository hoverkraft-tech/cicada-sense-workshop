# UAT ArgoCD applications

This folder contains ArgoCD `Application` manifests for workloads deployed to the user acceptance testing environment.

Each subfolder usually maps to one application and defines the deployment configuration used to validate releases before production promotion.

Use this folder for UAT-specific application definitions and release validation flows.
