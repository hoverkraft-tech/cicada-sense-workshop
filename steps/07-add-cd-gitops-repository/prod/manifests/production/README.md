# Production raw manifests

This folder contains raw Kubernetes manifests for the production environment.

Store production-specific resources here when they are managed directly as manifests instead of being fully expressed through Helm values or higher-level ArgoCD application configuration.

Only place production-targeted resources in this folder.
